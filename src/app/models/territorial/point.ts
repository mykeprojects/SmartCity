export interface Point {
    "id_annotation" ?: number,
    "id_neighborhood" ?: number,
    "id_point" ?: number,
    "latitude" ?: number,
    "longitude" ?: number,
    "order" ?: number,
    "point_type": string
}
